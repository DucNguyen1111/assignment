import ImportImagePage from '../../libs/ui/pages/ImportImagePage';
import CustomLayout from '../../libs/ui/layout/Layout';

const Home = () => {
  return (
    <CustomLayout>
      <ImportImagePage />
    </CustomLayout>
  );
};

export default Home;
