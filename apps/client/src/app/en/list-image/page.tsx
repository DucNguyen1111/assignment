import CustomLayout from '../../../libs/ui/layout/Layout';
import ListImagePage from '../../../libs/ui/pages/ListImagePage';

const ListImage = () => {

  return (
    <CustomLayout>
      <ListImagePage />
    </CustomLayout>
  );
};

export default ListImage;
